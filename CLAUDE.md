# CLAUDE.md — cc-PortfolioKit 開発ガイド

このファイルは Claude Code がこのリポジトリで作業するときに必ず参照するルールです。

---

## このプロジェクトについて

**cc-PortfolioKit** は、非エンジニアでも自分のポートフォリオ ── つまり自己紹介ページ ── を作れることを目指した Claude Code プラグインです。

- 操作コマンドは **メインの 2 本 + 道案内の 1 本** で構成する。
  - **`init`**: 最初の 1 回だけ叩く **対話セットアップ専用** コマンド。氏名・キャリア年数・職歴を対話形式でヒアリングして Markdown に書き込む。HTML は生成しない。
  - **`create`**: ふだん使いのコマンド。`portfolio.html` が未生成なら新規生成、既存なら編集メニューを起動 ── 旧 `update` に相当する役割です。素材が無い場合 ── つまり `init` がまだ実行されていない場合は、「先に `init` を実行して」と案内する。テンプレートを勝手に作らない。
  - **`guide`** ── 「お守り」コマンドです。cwd を診断して「次に打つコマンドを 1 つだけ」教える **読み取り専用の道案内**。何も書き換えない / 代理実行しない。初めての人や久しぶりに使う人のための安全網。
- `update` のような別コマンドは設けない。役割は `create` に統合済み。`init` と `create` は役割がはっきり分かれているので、使い分けで迷うことはない。`guide` はあくまでメインフロー外の補助。
- **デザイン責務は専用スキルに分離**: HTML 構造 / CSS / 配色パレット / 職業別ラベル / Markdown 変換 / XSS 対策 は `skills/portfolio-design/SKILL.md` が単独で受け持つ。`create.md` は HTML 生成時にこのスキルを `Skill` ツールで呼び出す。デザインの仕様変更は **必ずスキル側** で行い、`create.md` 側に HTML テンプレや CSS 詳細を逆流させないこと。
- 詳細は `docs/` 配下の仕様書を参照すること。

---

## ターゲットユーザー (最重要)

ターゲットは **非エンジニア** です。次を常に意識すること:

- 実装・ドキュメント・エラーメッセージ・対話文すべてにおいて **専門知識を前提にしない**。
- 専門用語を避ける。やむを得ず使う場合は **必ず一言添える**。書き方は下の「補足の書き方ルール」に従う。
- 「これくらい分かるだろう」を排除し、**一から教えるつもりで** 書く。

---

## 文体のトーン (重要)

ドキュメント・対話文は **堅苦しい説明だけで終わらせず、適度にユーモアを混ぜる** こと。

- 比喩・軽口・親しみのある言い回しを混ぜて、読み手の肩の力を抜く。たとえば「コマンドという呪文」「黄色いアイコンのあれ」のような表現です。
- ただし **やりすぎない**。ふざけて情報が埋もれたり、読者を見下す印象になるのは絶対 NG。
- 情報の **正確さは絶対に削らない**。ユーモアは枕詞や合間の一言として添える。
- 「読み物として読んでて楽しい技術書」をイメージするとちょうどよい。

---

## 補足の書き方ルール (重要)

ドキュメント・対話文では、**「補足説明を `( )` の中に入れる」スタイルを避ける** こと。「専門用語のあとに `( )` で意味を添える」「文の途中で `( )` で言い換えを挟む」「`(例: …)`」「`(参考: …)`」のような書き方は禁止です。

### Why

- 読み手 ── このプラグインの場合は非エンジニア ── が `( )` の中を「読まなくていい注釈」と感じて飛ばしてしまうか、逆に流れが切れて読みづらくなる
- 地の文として書いた方が内容が記憶に残りやすく、優しい印象になる
- ドキュメント全体の文体も統一されやすい

### やめる書き方 → 推奨する書き方

| 避けたい書き方 | 推奨する書き方 |
|---|---|
| `HTML ファイル (ブラウザで開けるファイル)` | `HTML ファイル ── つまりブラウザで開けるファイルのことです` |
| `Markdown (記号で見た目を整える文章フォーマット)` | `Markdown という、記号で見た目を整える文章フォーマット` |
| `コマンド (Claude Code への呪文)` | `コマンドというのは、Claude Code への呪文みたいなものです` |
| `(例: 山田 太郎)` | `たとえば「山田 太郎」のような形式` |
| `(参考: docs/手順書.md)` | `詳しくは docs/手順書.md を参照` |
| `配色 (色の組み合わせ) を選ぶ` | `配色 ── つまり色の組み合わせ ── を選ぶ` |

### 3 つの置き換えパターン

1. **「つまり」「という」「ということ」で繋ぐ**: `Markdown という記号文章フォーマット` / `Markdown、つまり〜のこと`
2. **別の文に分ける**: `Markdown を使います。Markdown は記号で見た目を整える文章フォーマットです`
3. **「たとえば」「詳しくは」など説明開始の語で始める**: `たとえば、〜の場合は…` / `詳しくは〜を参照`

ダッシュ `──` で繋ぐ書き方も使えますが、1 つの段落に 2 個以上は入れないこと。多用すると見にくくなります。

### やりすぎ注意 — `( )` すべてを潰すのが目的ではない

次の `( )` は **対象外**。手を入れなくて OK:

- **構造的なラベル**: 見出しに付く `(最重要)` `(重要)` `(再掲)` `(任意)` `(必須)` `(推奨)` のような重要度・属性タグ
- **番号付きリスト**: `(1)` `(2)` のような番号
- **ファイル名・コマンド名に元から含まれる ()**
- **著者名・引用元・ライセンス表記など形式的な ()**
- **半角の () を含むコード例や JSON ・ YAML 例**

**「補足説明を `( )` の後付けで書く」スタイルだけ** をやめる、と覚えてください。

---

## 並行セッションへの対応 (重要)

このリポジトリは **複数の Claude Code セッションが同じファイルを並行して編集している前提** で作業してください。同僚と同じ Google Doc を共同編集しているのと同じ感覚です。気づかずに上書きすると、他のセッションが書いた内容が消えてユーザーがやり直しになります。

### ルール 1. 編集の直前に必ず `Read` し直す

会話の前半で読んだ記憶のまま `Edit` や `Write` に行かないこと。**「Edit する 1 ステップ前に、もう一度 `Read`」** を癖にしてください。Read を挟んで、ファイルがいま自分の想定通りの状態か必ず確認します。

### ルール 2. 想定外の差分は「他セッションの意図ある変更」とみなす

ファイルが自分の想定と違う形になっていたら、それは **他のセッションが意図して入れた変更** だとみなします。勝手に元に戻さない。「あれ、これ消えてる、戻すぞ」と動かず、まずは尊重する。

### ルール 3. 不明な変更が見つかったらユーザーに確認

「なぜこれが変わっているのか分からない」箇所がある場合は、**ユーザーに「これはどう扱いますか?」と確認してから進める**。憶測で上書きしないこと。

### ルール 4. ディレクトリの増減もチェック

`Bash` の `ls` などで `commands/` / `templates/` / `docs/` / `.claude-plugin/` を毎回ざっと見直す。**ファイルが消えていたり、新しく増えていたり** することがあります。ファイル名の変更も含めて気にかけること。

### ルール 5. こまめにコミット (再掲)

[修正時の必須ルール](#修正時の必須ルール) の「ステップ 3. 細かい修正単位でこまめにコミット」を確実に守ってください。並行セッションがあるときは、衝突範囲を最小化することが事故防止のほぼすべてです。

> **メモ:** これは「他のセッションを信用するな」という話ではなく、「**自分の手元の理解が古いかもしれない前提で動こう**」という話です。Google Docs を共同編集するときのマナーと同じ。

---

## 修正時の必須ルール

このプロジェクトに **何らかの修正** ── コード追加、テンプレート変更、コマンド挙動変更、設定変更などです ── を加える場合は、**必ず次の各ステップを実行** してください。**例外なし** です。

### ステップ 1. `docs/` 配下の仕様書を併せて更新する

修正内容が反映されるよう、`docs/` 配下の仕様書を同期更新します。**コードと仕様書がずれた状態で作業を完了させないこと**。

- 新機能を追加したら、その仕様を `docs/` に追記または新規ファイルとして作成する。
- 既存挙動を変えたら、該当する仕様書の記述も書き換える。

### ステップ 2. 手順書 (`docs/手順書.md`) を更新する

非エンジニアのユーザー向けの手順書を更新します。判定ルールは次のとおり:

- **`docs/手順書.md` が存在する場合** → 修正内容を反映して **更新** する。
- **`docs/手順書.md` が存在しない場合** → **新規に生成** する。

そして **修正を加えるたびに、必ず手順書も更新** すること。修正と手順書の更新を分けて作業しない。

### ステップ 3. 細かい修正単位でこまめにコミットする

**細かく修正が入るたびに、細かくコミットしてください。** 例外なし。

- 1 つの論理的な修正ごとにコミットを切る。たとえば「1 つの機能追加」「1 つのファイル書き換え」「1 つのバグ修正」がそれぞれ 1 コミット。
- 複数の独立した修正をまとめて 1 コミットにしない。あとで巻き戻しがしにくくなるからです。
- 仕様書 `docs/` と手順書 `docs/手順書.md` の同期更新も **同じコミットに含める** こと。コードと仕様がずれた中間状態をコミットしないためです。
- コミットメッセージは **何をなぜ変えたか** が分かる短い 1 行を心がける。たとえば `create.md: テーマ決めウィザードを追加` のような書き方です。
- このリポジトリは **複数の Claude Code セッションが並行して作業する可能性がある**。こまめなコミットは衝突回避と巻き戻し容易性のために重要です。

### ステップ 4. ある程度コミットが固まったらリモートに push する

**必要に応じて、ある程度コミットが固まったタイミングで `git push` を実行** してください。毎コミットごとに push する必要はありません。

- 目安は **2〜5 コミット** 溜まったとき、または **1 つの機能・修正の節目** に達したとき。
- 逆に **10 コミット以上ローカルに溜め込まない** こと。並行セッションがリモートを参照することがあるので、長期間ローカル滞留させないためです。
- push の前に `git fetch` または `git pull --rebase origin main` でリモートの最新を取り込み、自分のローカルとリモートを揃えてから push する。`git push --force` は原則禁止です。
- **「壊れたまま push しない」**: テスト中・検証中の中途半端な状態は push を保留し、ユーザーに「ここまでで push して大丈夫?」と確認すること。
- ユーザーから明示的に「まだ push しないで」と指示された場合は従う。

---

## 手順書の書き方ガイドライン

`docs/手順書.md` を作成・更新するときは次を厳守:

- **読み手は非エンジニア** と想定する。「コマンドラインって何?」レベルの方も含む。
- **専門用語は使わない**。やむを得ず使う場合は **必ず一言添える**。書き方は上の「補足の書き方ルール」に従い、`( )` で囲むのではなく地の文として書く。
- **一から教える** つもりで、順を追って具体的に書く。「○○を準備してください」で終わらせず、「どこに何を置くか」まで書く。
- **実際に入力する文字** や **画面に出る言葉** を具体的に書く。たとえば「`/cc-portfoliokit:create` と入力」のような形です。
- 「**もしうまくいかなかったら**」のフォローを各ステップに添える。
- 命令口調を避け、丁寧で温かみのある言い回しを使う。
- 必要であれば **絵文字を控えめに** 使って読みやすくしてもよい。ただし多用しないこと。

---

## ファイル / ディレクトリ構成 — 現時点

```
cc-PortfolioKit/
├── CLAUDE.md                    # このファイル
├── README.md
├── .claude-plugin/
│   ├── plugin.json
│   └── marketplace.json
├── commands/                    # スラッシュコマンドの定義
│   ├── init.md                  # 対話セットアップ。1 回限り
│   ├── create.md                # HTML 生成と編集メニュー。ふだん使い
│   └── guide.md                 # 現状診断と次の一手の道案内。読み取り専用
├── skills/                      # コマンドから呼び出される専門スキル
│   └── portfolio-design/
│       └── SKILL.md             # HTML / CSS / 配色 / 職業別ラベル / XSS など、デザイン一切の単一情報源
├── templates/                   # ユーザーが編集する Markdown 雛形
│   ├── config.md
│   ├── profile.md
│   ├── work-history.md
│   └── assets-README.md
└── docs/
    ├── 手順書.md                 # 非エンジニア向けユーザー手順書 ── 使い方の本編
    ├── はじめてのClaude Code.md   # Claude Code 未経験者向けの導入ガイド、CLI 編
    ├── デスクトップアプリで使う.md # デスクトップアプリ版での導入手順
    ├── トラブルシューティング.md   # エラー対応・困ったときの解消ガイド
    └── specs/                   # 機能仕様・設計メモ
        └── create-command.md    # create コマンドの機能仕様
```

---

## チェックリスト — 作業完了前に確認

修正作業を終える前に、次をすべて満たしているか確認:

- [ ] 修正内容が `docs/` 配下の仕様書に反映されているか
- [ ] `docs/手順書.md` が更新されているか、無ければ新規生成されているか
- [ ] 手順書が非エンジニアにとって読みやすい言葉になっているか
- [ ] 専門用語にはすべて補足が付いているか、かつ `( )` ではなく地の文で書かれているか
- [ ] 細かい修正単位で **こまめにコミット** されているか。まとめコミットになっていないか
- [ ] コミットが溜まっていれば **リモートに push** されているか。10 コミット以上ローカル滞留していないか
